# CryptoVulnX - Environment (HISTORICAL - 2025-Q4)
# VULN (API8): Archivo aun mas viejo. Los devs nunca limpian.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=cryptovulnx_dev
DB_USER=devuser
DB_PASS=dev2025!

JWT_SECRET=jwt_secret_2025_q3
DEBUG=true
APP_ENV=development

# Comentario interno de un dev (no borrado):
# "TODO: rotar JWT_SECRET cuando salgamos de beta - se que esta debil"
# "TODO: mover .env fuera del docroot"
# "TODO: bloquear /api/v3/ y /api/internal/ desde Apache"

# Master credentials (compartidas internamente, nunca rotadas)
MASTER_DB_USER=root
MASTER_DB_PASS=toor
MASTER_API_KEY=master_api_key_static_do_not_change

# Endpoint de health interno (no documentado)
INTERNAL_HEALTH_URL=http://10.0.1.50:8080/api/v1/internal/health
INTERNAL_BACKUP_URL=http://10.0.1.50:8080/api/v1/admin/backup.php
